# Generated by .brand/gen_repo_build.py — regenerate rather than hand-edit.
#
# Builds this app's images from the source in this directory. Used by ./build.sh;
# the server never needs this file, only compose.yaml.

name: techyst-15-postiz
services:
  postiz:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-postiz-postiz:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.techyst
      cache_from:
      - ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-postiz-postiz:latest
