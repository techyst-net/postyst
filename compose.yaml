# Generated by .brand/gen_repo_compose.py — regenerate rather than hand-edit.
#
#   infisical export --projectId techyst-15-postiz --env prod --format dotenv > .env
#   docker compose up -d
#
# Images come from ghcr.io; `docker login ghcr.io` first. Databases, cache,
# queue and object storage are all external, so nothing here holds state and
# `docker compose down` is safe.

name: techyst-15-postiz
services:
  postiz:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-postiz-postiz:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    ports:
    # 4200 is the Next frontend, 3000 the NestJS API. Both are published
    # because api.postyst.techyst.net has to reach the API directly; routing it
    # at 4200 lands on the frontend, which answers without CORS headers.
    - 3060:4200
    - 127.0.0.1:3061:3000
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
